# Python
__pycache__/
*.pyc
.venv/
venv/

# data & model artifacts — never commit datasets or trained weights
# (downloaded GTSRB lives in training/gtsrb-data/, kept out of training/data/
# so it doesn't collide with the training/data/ *code* package)
/**/gtsrb-data/
*.pt
*.pth
*.onnx
*.engine
mlruns/

# secrets
*.env
.env
*key.json
kubeconfig*

# IDE
.idea/
.vscode/

# OS
.DS_Store
